# 系统架构设计

## 总览

```
┌─────────────────────────────────────────────────────────────────┐
│                        外部训练语料                               │
│                    (自然语言文本, 如 WikiText)                     │
└───────────────────────────┬─────────────────────────────────────┘
                            │
                            ▼
┌─────────────────────────────────────────────────────────────────┐
│                     第 0 轮：标准 LM 训练                         │
│              Model_0 ← 训练 ← 自然语言语料                        │
│              获得基础语言能力                                      │
└───────────────────────────┬─────────────────────────────────────┘
                            │
                            ▼
┌─────────────────────────────────────────────────────────────────┐
│                     第 K 轮迭代循环                               │
│                                                                   │
│  ┌──────────┐    ┌──────────────┐    ┌──────────────────────┐  │
│  │ Model_K  │───▶│ Token 级复述  │───▶│ 内部语言数据集 D_K   │  │
│  │          │    │ (带内部token) │    │ (内部Token序列)      │  │
│  └──────────┘    └──────────────┘    └────────┬─────────────┘  │
│       ▲                                       │                 │
│       │                               ┌───────▼──────────┐    │
│       │                               │  联合训练          │    │
│       │                               │  Model_{K+1} ← D_K│    │
│       │                               │  + 下游任务损失    │    │
│       │                               │  + 防坍塌正则化    │    │
│       │                               └───────────────────┘    │
│       │                                                         │
│       └─────────────────────────────────────────────────────────┘
│                            │
│  ┌─────────────────────────▼──────────────────────────────┐    │
│  │              翻译模型 Translator_K                       │    │
│  │     内部语言 ──────▶ 自然语言                             │    │
│  │     训练方式：回译 + 循环一致性损失                        │    │
│  └─────────────────────────────────────────────────────────┘    │
└─────────────────────────────────────────────────────────────────┘
```

## 一、Token 体系设计（核心创新点）

### 1.1 分词器扩展

在标准 GPT-2 tokenizer（50257 tokens）基础上扩充内部 token 词汇表：

```
总词汇表 = 自然语言 tokens (50257) + 内部 tokens (4096) + 特殊 tokens (8)
         = 54361 tokens
```

特殊 token 定义：

| Token | ID | 用途 |
|-------|-----|------|
| `<INTERNAL_START>` | 50257 | 标记内部语言序列开始 |
| `<INTERNAL_END>` | 50258 | 标记内部语言序列结束 |
| `<INTERNAL_0>` ~ `<INTERNAL_4095>` | 50259 ~ 54354 | 内部语言词汇表 |
| `<NL_START>` | 54355 | 标记自然语言序列开始 |
| `<NL_END>` | 54356 | 标记自然语言序列结束 |
| `<PAD>` | 54357 | 填充 token |
| `<TASK_MATH>` | 54358 | 数学推理任务标记 |
| `<TASK_LOGIC>` | 54359 | 逻辑推理任务标记 |
| `<TASK_CODE>` | 54360 | 代码生成任务标记 |

### 1.2 "Token 级复述"的操作定义

**输入格式**：
```
<INTERNAL_START> [原始文本] <INTERNAL_END>
```

模型需要在 `<INTERNAL_START>` 和 `<INTERNAL_END>` 之间自由采样（只允许 internal token 范围内的采样），输出一个内部 token 序列：

```
<INTERNAL_START> <INT_42> <INT_7> <INT_1337> ... <INTERNAL_END>
```

**关键约束**：
- 在 internal 区域内，logits 只保留 4096 个 internal token（mask 掉自然语言 token）
- 采样温度：初始 T=1.2（鼓励多样性），后期逐渐降低
- 每个样本的最大 internal token 数 = min(原文 token 数 × 3, 2048)，允许模型比原文长

## 二、模型架构

### 2.1 主模型（Language Model）

| 参数 | 值 |
|------|-----|
| 参数量 | ~135M |
| 架构 | GPT-2 风格 Decoder-only Transformer |
| 层数 | 12 |
| 隐藏维度 | 768 |
| 注意力头数 | 12 |
| FFN 维度 | 3072 |
| 词汇量 | 54361 |
| 最大序列长度 | 2048 |
| 位置编码 | RoPE |

### 2.2 翻译模型（Internal → Natural Language）

| 参数 | 值 |
|------|-----|
| 参数量 | ~50M |
| 架构 | Encoder-Decoder Transformer |
| Encoder 层数 | 6 |
| Decoder 层数 | 6 |
| 隐藏维度 | 512 |
| 注意力头数 | 8 |
| 输入词汇表 | 54361（含 internal tokens） |
| 输出词汇表 | 50257（仅自然语言 tokens） |

## 三、训练循环

### 3.1 损失函数

```
L_total = L_rec + α·L_trans + β·L_task + γ·L_div
```

| 损失项 | 权重 | 描述 |
|--------|------|------|
| `L_rec` | 1.0 | 内部语言自回归重建损失（CE loss） |
| `L_trans` | α=0.3 | 翻译模型回译损失（MSE） |
| `L_task` | β=0.5 | 下游推理任务损失（CE loss） |
| `L_div` | γ=0.1 | 多样性正则化（惩罚低熵输出） |

### 3.2 每轮训练流程

```
第 K 轮：

1. 复述阶段
   - 加载 Model_K
   - 对训练集的每个样本，生成内部 token 序列
   - T = 1.2（采样），top_p = 0.95
   - 输出：D_internal_K（内部语言数据集）

2. 训练阶段
   - 初始化 Model_{K+1} = 随机初始化（或从 Model_K 继承）
   - 在 D_internal_K 上训练，优化 L_total
   - 同时训练 Translator_K
   - 评估：val loss、token 多样性、翻译 BLEU

3. 混合阶段
   - 从原始语料随机采样 20%，混入 D_internal_K
   - 防止完全偏离自然语言分布

4. 评估阶段
   - 下游任务准确率（数学/代码/逻辑）
   - 内部 token 的 entropy 分布
   - 翻译保真度（BLEU / BERTScore）
   - Token 使用率（实际使用了多少 internal token）

5. 继续下一轮
```

### 3.3 超参数

| 参数 | 值 |
|------|-----|
| 每轮训练步数 | 10K steps |
| 总迭代轮数 | 20 轮 |
| Batch size | 64 |
| 学习率 | 3e-4（warmup 1K steps, cosine decay） |
| 采样温度 | 1.2 → 0.8（线性衰减） |
| 数据混合比例 | 20% 原始数据, 80% 内部语言 |
| Entropy bonus 系数 | 0.05 |

## 四、评估体系

### 4.1 核心指标

| 指标 | 含义 | 期望趋势 |
|------|------|----------|
| 下游任务准确率 | 内部语言是否有利于推理 | ↑ |
| Token 熵 (H) | 内部语言的信息密度 | 先 ↑ 后稳定 |
| 有效 token 使用率 | 4096 个 internal token 中实际使用了多少 | 稳定在 60-80% |
| 翻译 BLEU | 内部语言→自然语言的可解码性 | > 30 |
| 压缩率 | 内部 token 数 / 原文 token 数 | < 1.0 |
| 内部语言自洽性 | 同一输入多次复述的一致性 | > 0.7 |

### 4.2 下游任务

| 任务 | 数据集 | 指标 |
|------|--------|------|
| 数学推理 | GSM8K (简化版) | 准确率 |
| 代码生成 | MBPP (简化版) | Pass@1 |
| 逻辑推理 | LogiQA (子集) | 准确率 |
| 语言理解 | LAMBADA | Perplexity |

### 4.3 对照实验

| 实验组 | 描述 | 目的 |
|--------|------|------|
| **Full** | 完整方案（internal tokens + 迭代 + 推理损失） | 核心验证 |
| **No-Internal** | 用自然语言复述代替 internal token 复述 | 验证 internal token 的必要性 |
| **No-Task** | 去掉推理任务损失（仅重建） | 验证推理信号的必要性 |
| **No-Iter** | 仅 1 轮（不迭代） | 验证迭代的必要性 |
| **Random-Internal** | 用随机 internal token 替换（不训练直接随机） | 下界 baseline |

## 五、技术栈

| 组件 | 推荐 |
|------|------|
| 框架 | PyTorch 2.x + HuggingFace Transformers |
| 分布式训练 | 单卡（4090），可选 FSDP |
| 监控 | Weights & Biases / TensorBoard |
| 分词器 | GPT-2 tokenizer (tiktoken 备选) |
| 评估 | lm-evaluation-harness |
| 配置管理 | Hydra / YAML |
| 实验管理 | MLflow |
